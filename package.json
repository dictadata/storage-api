{
  "name": "@dictadata/storage-api",
  "version": "0.5.0",
  "description": "Example REST API server extending @dictadata/storage-node.",
  "author": "Drew O. Letcher",
  "license": "MIT",
  "main": "src/main.js",
  "repository": "github:dictadata/storage-api",
  "scripts": {
    "debug": "set NODE_ENV=development& node --inspect-brk --preserve-symlinks src/main.js",
    "lint": "eslint src/**/*.js",
    "lint-fix": "eslint --fix src/**/*.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@dictadata/storage-junctions": "^0.5.0",
    "@dictadata/storage-node": "^0.5.0",
    "express": "^4.17.1",
    "formidable": "^1.2.1",
    "nodemailer": "^6.3.1"
  },
  "devDependencies": {
    "eslint": "^5.16.0",
    "eslint-plugin-node": "^9.1.0",
    "eslint-plugin-promise": "^4.2.1"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}
