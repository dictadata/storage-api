{
  "name": "@dictadata/storage-api",
  "version": "1.2.2",
  "description": "REST API server that extends from @dictadata/storage-node.",
  "author": "Drew O. Letcher",
  "license": "MIT",
  "main": "lib/main.js",
  "repository": "github:dictadata/storage-api",
  "scripts": {
    "debug": "node --inspect --preserve-symlinks lib/main.js",
    "lint": "eslint lib/**/*.js",
    "lint-fix": "eslint --fix lib/**/*.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=15.0.1"
  },
  "dependencies": {
    "@dictadata/storage-junctions": "^1.2.8",
    "@dictadata/storage-node": "^1.2.2",
    "express": "^4.17.1",
    "formidable": "^1.2.2",
    "nodemailer": "^6.4.17"
  },
  "devDependencies": {
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-promise": "^4.2.1"
  }
}
